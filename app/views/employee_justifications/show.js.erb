jQuery(document).ready(function($) {
  var css_class = '<%= @employee_justification.css_class %>';
  var preloader_html = '<div style="text-align: center;">' +
                          '<div class="preloader-wrapper big active">' +
                            '<div class="spinner-layer spinner-blue-only">' +
                              '<div class="circle-clipper left">' +
                                '<div class="circle"></div>' +
                              '</div><div class="gap-patch">' +
                                '<div class="circle"></div>' +
                              '</div><div class="circle-clipper right">' +
                                '<div class="circle"></div>' +
                              '</div>' +
                            '</div>' +
                          '</div>' +
                        '</div>';
  $("." + css_class).html(preloader_html);
  $("." + css_class).html(" <%= j render('show', employee_justification: @employee_justification, employee: @employee)%>");
  initPlugins($("." + css_class));
});