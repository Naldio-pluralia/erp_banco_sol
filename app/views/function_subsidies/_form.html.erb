<%= simple_form_for(function_subsidy) do |f| %>
  <%= form_url_helpers(f)%>
  <div id="function_subsidy-modal-form" class="modal">
    <div class="modal-content">
      <span class="h5-size"> <%= function_subsidy.new_record? ? 'Adicionar Subsídio' : 'Actualizar Subsídio' %> </span>
      <hr>

      <div class="flex-row flex-top">
        <%= f.input :name, wrapper_html: {class: 'flex-col-xs-12'}, input_html: {class: ''} %>
      </div>
    </div>

    <div class="modal-footer">
      <%= f.button :submit %>
      <%= link_to t('cancel'), function_subsidy.cancel_url || function_subsidies_path, class: 'waves-effect waves-light btn btn-flat btn-cancel-form' %>
    </div>
  </div>
<% end %>
