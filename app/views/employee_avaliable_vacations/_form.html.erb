<%= simple_form_for(employee_avaliable_vacation) do |f| %>
  <%= form_url_helpers(f)%>
  <div id="employee_avaliable_vacation-modal-form" class="modal modal-fixed-footer">
    <div class="modal-content">
      <span class="h5-size"> <%= employee_avaliable_vacation.new_record? ? view_t('.new_x', x: view_t('.employee_avaliable_vacation')) : view_t('.edit_x', x: view_t('.employee_avaliable_vacation')) %> </span>
      <hr>

      <div class="flex-row flex-top">
        <%= f.association :employee, wrapper_html: {class: 'flex-col-xs-4'}, input_html: {class: ''} %>
        <%= f.input :date, wrapper_html: {class: 'flex-col-xs-4'}, input_html: {class: ''} %>
        <%= f.input :number_of_days, wrapper_html: {class: 'flex-col-xs-4'}, input_html: {class: ''} %>
        <%= f.input :valid_until, wrapper_html: {class: 'flex-col-xs-4'}, input_html: {class: ''} %>
      </div>
    </div>

    <div class="modal-footer">
      <%= f.button :submit %>
      <%= link_to t('cancel'), employee_avaliable_vacation.cancel_url || employee_avaliable_vacations_path, class: 'waves-effect waves-light btn btn-flat btn-cancel-form' %>
    </div>
  </div>
<% end %>
