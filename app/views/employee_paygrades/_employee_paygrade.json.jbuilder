json.extract! employee_paygrade, :id, :level, :paygrade, :since, :until, :employee_id, :paygrade_change_reason_id, :created_at, :updated_at
json.url employee_paygrade_url(employee_paygrade, format: :json)
