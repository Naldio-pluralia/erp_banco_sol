json.extract! application, :id, :name, :email, :cellphone, :birthdate, :civil_status, :dependents_number, :note, :status, :relevance, :application_id, :employee_id, :created_at, :updated_at
json.url application_url(application, format: :json)
